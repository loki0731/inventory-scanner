DATABASE_URL=postgresql+asyncpg://scanner:scanner@postgres:5432/inventory
API_TOKEN=change-me
# base64-encoded 32-byte key; generate with: python -c "import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
CREDENTIAL_ENCRYPTION_KEY=change-me
SCANNER_ID=scanner-01
SCANNER_VERSION=1.0.0
MAX_CONCURRENT_SCANS=5
SSH_CONNECT_TIMEOUT=10
WINRM_CONNECT_TIMEOUT=15
COMMAND_TIMEOUT=30
SCAN_TIMEOUT=300
SCHEDULER_INTERVAL=60
